# Problem   : Range LCM Queries
# Difficulty: Medium
# Tags      : [object Object]
# Language  : Python
# Date      : 2026-05-12
# ───────────────────────────────────────────────────────
# O(N log A + Q log N log A) where A is max value in array, or O(N + Q log N) if GCD/LCM are O(1) for Python's arbitrary precision integers.
# O(N)
import math

#
