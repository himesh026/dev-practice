# Problem   : Remove Invalid Parentheses
# Difficulty: Hard
# Tags      : [object Object]
# Language  : Python
# Date      : 2026-05-07
# ───────────────────────────────────────────────────────
# Time complexity: O(N * 2^N) in the worst case, where N is the length of the input string.
# This is because in the worst case, we might explore up to 2^N substrings.
# For each substring, we perform string slicing (
