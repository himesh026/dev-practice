// Type      : Backend Utility
// Date      : 2026-05-18
// ───────────────────────────────────────────────────────
/**
 * @typedef {import('socket.io').Server} SocketIOServer
 * @typedef {import('socket.io').Socket} SocketIOSocket
 */

/**
 * A map to store online users per room: Map<roomName, Set<socketId
